# CompactCapture 引き継ぎメモ

## 現在の版

- バージョン: 1.19
- ビルド: 23
- 対応OS: iOS 17.0以降
- 構成: SwiftUI / AVFoundation / Photos
- プロジェクト: `CompactCapture.xcodeproj`

## アプリの目的

写真・動画の解像度、FPS、ビットレート、色の細かさ、コントラストなどを利用者が直接指定し、保存容量とピクセル表現を制御する撮影アプリ。

## 実装済みの主要機能

- 写真
  - PNG / JPEG / HEIF
  - PNGを初期値として保存
  - 最大解像度から0.025 MPまで選択
  - RGB各色を標準、64、32、16、8、4段階へ減色
  - コントラストを低／弱／標準／強／高の5段階で調整
  - PNGの1× / 2× / 4× / 8× / 16×最近傍拡大保存
  - JPEG / HEIF圧縮品質
- 動画
  - 4Kから160×90まで選択
  - 5 / 10 / 15 / 24 / 30 fps
  - HEVC / H.264、対応端末ではProRes 4444
  - ビットレート、色差、色の細かさ、コントラスト、音声有無
  - 最近傍整数倍出力
  - 公開APIと独自音源による固定の録画開始音・終了音（開始音後に録画開始、録画停止後に終了音）
- 撮影操作
  - ISOとシャッターを個別固定
  - 1/3段と1段の操作
  - 露出補正
  - WBプリセットまたは色温度指定
  - 手ぶれ補正、レンズ補正、HDR、低照度処理などを個別設定
  - 画面は縦固定のまま、写真はシャッター時、動画は録画開始時の端末方向を保存結果へ反映
- ズーム
  - 対応端末で0.5×超広角、広角、望遠を使用
  - 0.5× / 1× / 2× / 3× / 5× / 10× / 15×を基準に、端末構成へ合わせた最大7個の代表倍率ボタン
  - 最大15×、横スクロールなしの代表倍率選択と、画面ピンチによる連続微調整
  - 倍率ボタンは待ち時間のあるランプ動作を使わず即時適用
  - レンズ境界にヒステリシスを設け、境界付近での頻繁な物理レンズ往復を抑制
  - 物理レンズ切替によりISO・シャッターの手動設定を維持
  - 録画中は現在の物理レンズ内だけで連続ズーム
  - ズーム倍率を保存
- 表示と保存
  - 選択した解像度、色の細かさ、コントラストを反映したライブ結果プレビュー
  - アプリ専用領域への写真・動画保存
  - 任意で写真アプリへ追加保存（読み取り権限は不使用）
  - 実ファイル容量と設定ごとの推定容量を表示
  - アプリ内ギャラリー、動画再生、最近傍写真表示
  - ピンチ、ダブルタップ、倍率ボタンによる閲覧ズーム
  - 写真・動画間の左右スワイプと前後ボタン
  - 詳細表示から1操作で撮影画面へ復帰
- UI
  - 撮影と設定を分離
  - 撮影画面上部の状態メッセージと「保存結果の目安」を非表示
  - 撮影画面下部のパネルとズーム欄もすりガラスぼかしを廃止し、透明度の高い単色背景を使用
  - 前回撮影した実ファイル容量は撮影画面へ表示せず、録画中・処理中の状態だけを下部パネル内に表示
  - 設定項目を画面下側へ配置
  - 設定画面でもカメラを停止せず、ライブ映像へ設定変更を即時反映
  - 設定画面上部の重複するプレビュー説明・要約と、最下部の容量説明を非表示
  - 設定、一覧、詳細表示の最下段全体を「撮影へ」ボタンとして統一
  - 詳細表示の写真ズーム・動画再生UIには下部余白を設け、「撮影へ」との重なりを防止
  - 低解像度プレビューを論理解像度どおりの画素数で表示
  - レンズ切替中の不安定なフレームを除外し、プレビューを縦向きへ正規化
  - カメラ回転コーディネーターで撮影時の重力方向を取得し、写真のExif方向と動画トラック変換へ反映
  - 設定項目のすりガラスぼかしを廃止し、透明度の高い単色背景と暗い文字縁取りを使用
  - 「カラー深度」の表示名を一般向けの「色の細かさ」へ変更
  - 写真マークを主役に、左から右へ段階的に解像度が落ちる厳密な3色構成、1024×1024・不透明背景の仮App Iconを設定
  - 現在の撮影設定を名前付きプリセットとして保存・同名更新・適用・削除
  - 設定値を自動保存

詳細仕様は `README_ja.md` を参照。

## 主なファイル

- `ContentView.swift`: 撮影、設定、ギャラリー、閲覧UI
- `CameraController.swift`: セッション、露出、WB、ズーム、撮影、設定保存
- `CaptureModels.swift`: 設定モデルと永続化モデル
- `PhotoProcessor.swift`: 写真の縮小、減色、最近傍拡大、エンコード
- `VideoRecorder.swift`: 動画変換とAVAssetWriter記録
- `LivePreviewProcessor.swift`: 保存結果に近いライブプレビュー
- `AppPhotoLibrary.swift`: アプリ内保存、一覧、画像読込、動画再生
- `CameraPreview.swift`: AVCaptureVideoPreviewLayer

## 最新版でのズーム設計

- 仮想トリプル／デュアルワイドカメラはカスタム露出を利用できないため使用していない。
- 超広角、広角、望遠の物理カメラを検出し、表示倍率に応じて入力を切り替える。
- 物理レンズ間の切替時は一瞬プレビューが途切れる可能性がある。
- 動画録画中は物理レンズを切り替えず、現在のレンズで可能な範囲だけズームする。
- 端末構成に応じて倍率ボタンと利用可能範囲を自動生成する。
- 代表倍率は画面内に収まる最大7個へ制限し、ピンチで中間倍率を補う。

## 検証状況

- Release / iPhone実機向け（Assets除外・署名なし）Xcodeビルド: 合格
- 変更したSwiftファイルの構文解析: 合格
- 設定永続化、ズーム結線、物理カメラ選択の静的検査: 合格
- App Icon: 1024×1024、アルファなし、Asset Catalog JSON検証合格
- Info.plist解析: 合格
- ZIP整合性検査: 合格
- iOS実機でのカメラ動作確認は未実施
- この作業環境のSimulatorランタイム破損により、App Iconを含むAsset Catalogの最終コンパイルはXcode実機ビルド時に再確認する。

## 実機で優先確認する項目

1. 初回インストール後に一度で起動できるか。
2. 0.5×、1×、2×、望遠倍率の切替と保存結果が一致するか。
3. ISOまたはシャッター固定中に物理レンズを切り替えても設定が継続するか。
4. 動画録画中に、現在のレンズ内で代表倍率ボタンとピンチズームが動くか。
5. 録画中に別レンズの倍率ボタンがグレーアウトするか。
6. 低解像度・4段階/色・整数倍保存の写真と動画が意図したピクセル表現になるか。
7. 動画詳細画面の左右スワイプ、閲覧ズーム、撮影画面への直接復帰。
8. 小さい画面で撮影UIと設定UIが重ならないか。
9. 設定画面で画像サイズ、色の細かさ、コントラスト、色差を変更した直後にライブプレビューへ反映されるか。
10. 0.5×をまたぐレンズ切替でプレビューが一瞬横向きにならないか。
11. 0.05 MP / 0.025 MPを0.5×レンズで表示しても縦横比と画面占有率が安定するか。
12. 名前付きプリセットの保存、同名更新、適用、削除が再起動後も維持されるか。
13. 録画中の赤い状態表示と停止ボタンが明確に見えるか。
14. 端末を左右それぞれの横向きにして撮影した写真・動画が、写真アプリとアプリ内表示の両方で正しい向きになるか。
15. 横向きで録画を開始した後に端末を動かしても、動画全体が録画開始時の向きで安定して再生されるか。

## 既知の確認課題

- 新規インストール直後、最初の数回だけ起動直後に終了したという報告がある。再現時はXcodeの端末ログまたはクラッシュログが必要。
- `session.sessionPreset = .inputPriority` 周辺を含む起動時セッション構築は、クラッシュログ取得後の重点確認箇所。
- ProRes 4444、超広角、望遠の対応状況と最大倍率は端末によって異なる。
- HEVC / H.264では非可逆圧縮ノイズを完全には除去できない。
- AVAssetWriterによる独自録画を維持し、Appleの非公開音源ではなくアプリ独自の開始／終了音を使用する。録画表示は赤い点、ボタン形状、状態文も併用する。
- 現在のApp Iconは仮デザイン。正式ブランド決定後に同じAppIconセットの画像を差し替える。

## 開き方

1. ZIPを展開する。
2. `CompactCapture/CompactCapture.xcodeproj` をXcodeで開く。
3. Signing & Capabilitiesで開発Teamを選択する。
4. 必要ならBundle Identifierを変更する。
5. 実機iPhoneを選び、Build & Runする。
